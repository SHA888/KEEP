[project]
name = "keep"
version = "0.0.1"
description = "Implementation probe for prompt-injection attack defense"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.uv]
